import Todo from "./Todo";

const TodoList = () => {
  return (
    <div>
      {/* {todos.map((todo) => (
        <Todo key={todo.name} />
      ))} */}
      <Todo />
    </div>
  );
};

export default TodoList;
